# Autocheck — Микросервис интеграции и парсинга выгрузок 1С

Асинхронный микросервис на **FastAPI** для приема, парсинга и периодической синхронизации данных об автомобилях из выгрузок **1С** (форматы JSON и CSV). Поддерживает операцию **Upsert** (вставка новых записей и обновление существующих по уникальному ключу `VIN`), фоновый планировщик задач **APScheduler**, сохранение в **SQLAlchemy (Async)** и современный веб-интерфейс на **Jinja2**.

---

## 🚀 Быстрый старт

### Вариант 1. Запуск через Docker Compose (Рекомендуемый)

Для моментального развертывания со всеми зависимостями:

```bash
# Сборка и запуск контейнера в фоновом режиме
docker compose up --build -d

# Просмотр логов работы сервиса и планировщика
docker compose logs -f
```

После запуска сервис будет доступен:
- **Веб-интерфейс каталога**: [http://localhost:8000/](http://localhost:8000/)
- **Интерактивная документация Swagger**: [http://localhost:8000/docs](http://localhost:8000/docs)
- **Альтернативная документация ReDoc**: [http://localhost:8000/redoc](http://localhost:8000/redoc)

Для остановки:
```bash
docker compose down
```

---

### Вариант 2. Локальный запуск без Docker

Требуется **Python 3.10+**.

```bash
# 1. Создание и активация виртуального окружения
python3 -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate

# 2. Установка зависимостей
pip install --upgrade pip
pip install -r requirements.txt

# 3. Настройка переменных окружения (опционально)
cp .env.example .env

# 4. Запуск приложения через uvicorn
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

---

## 📁 Структура проекта

```text
autocheck/
├── app/
│   ├── __init__.py          # Инициализация пакета
│   ├── config.py            # Настройки сервиса (Pydantic Settings)
│   ├── database.py          # Подключение к БД (SQLAlchemy 2.0 Async + aiosqlite)
│   ├── models.py            # Модель Car (первичный ключ VIN)
│   ├── schemas.py           # Валидация входных полей 1С и схемы ответов
│   ├── services.py          # Логика парсинга JSON/CSV и атомарного Upsert
│   ├── scheduler.py         # Фоновый планировщик APScheduler
│   ├── main.py              # Точка входа FastAPI, Lifespan и маршрутизация
│   └── templates/
│       └── index.html       # Адаптивный веб-интерфейс каталога автомобилей
├── data/
│   └── 1c_dump.json         # Реалистичный датасет выгрузки из 1С
├── tests/
│   ├── __init__.py
│   ├── test_services.py     # Тесты валидатора, парсера и логики upsert
│   └── test_api.py          # Интеграционные тесты REST API и веб-страницы
├── Dockerfile               # Многослойная сборка контейнера на базе python:3.11-slim
├── docker-compose.yml       # Спецификация для docker compose с томами данных
├── requirements.txt         # Зависимости проекта
├── .env.example             # Пример конфигурации переменных окружения
├── .gitignore               # Игнорируемые файлы Git
└── README.md                # Документация проекта
```

---

## 🔄 Логика парсинга и Upsert

1. **Гибкость полей 1С**: Сервис автоматически распознает как русские названия полей (`VIN`, `Марка`, `Модель`, `Год`, `Пробег`, `Цена`, `Дефекты`), так и стандартные латинские названия.
2. **Атомарный Upsert**:
   - При парсинге файл считывается батчами.
   - По уникальному 17-значному идентификатору `vin` система проверяет наличие автомобиля в базе.
   - Если автомобиль уже есть в базе — актуализируются его стоимость, пробег, описание дефектов и дата обновления `updated_at`.
   - Если автомобиль новый — создается новая запись.
   - Дубликаты внутри одного файла объединяются (приоритет у последней записи).
3. **Форматы выгрузки**:
   - `JSON`: массив объектов либо объект с корневыми ключами (`cars`, `items`, `Выгрузка`, `Автомобили`).
   - `CSV`: автоматическое определение разделителя (запятая или точка с запятой), поддержка кодировок UTF-8 и CP1251 (Windows).

---

## ⏰ Фоновый планировщик (APScheduler)

В приложение встроен асинхронный планировщик `AsyncIOScheduler`:
- Периодически (по умолчанию каждые **5 минут**, настраивается параметром `PARSE_INTERVAL_MINUTES`) считывает актуальный файл по пути `data/1c_dump.json`.
- Автоматически синхронизирует базу данных без перезапуска сервиса.
- Если автосалон обновил файл на диске, новые цены и автомобили подтянутся в каталог автоматически.

---

## 🔌 REST API Маршруты

| Метод | Путь | Описание |
|---|---|---|
| `GET` | `/` | Главная HTML-страница каталога с фильтрацией, панелью дампов и статистикой |
| `GET` | `/api/cars` | Получить список автомобилей (фильтры `mark`, `search`, `min_year`, `max_year`, `min_price`, `max_price`, пагинация `limit`, `offset`) |
| `GET` | `/api/cars/{vin}` | Получить детальную информацию об автомобиле по VIN |
| `POST` | `/api/sync` | Принудительный запуск синхронизации (параметр `?file_name=1c_dump_base.json` для выбора конкретного файла из `data/`) |
| `GET` | `/api/dumps` | Список доступных файлов выгрузок в папке `data/` |
| `POST` | `/api/upload` | Ручная загрузка нового файла (JSON/CSV) через `multipart/form-data` |
| `GET` | `/api/status` | Статус здоровья сервиса и результат последней фоновой синхронизации |

---

## 📦 Тестовые наборы данных (`data/`)

В папке `data/` подготовлены реалистичные дампы с ценами в казахстанских тенге (₸):
1. `data/1c_dump_base.json` — Базовый набор из 10 популярных автомобилей (Toyota Camry, Tucson, Sportage, Cobalt, LC Prado, Lexus RX и др.).
2. `data/1c_dump_update.json` — Набор для наглядной проверки Upsert: скидка на Camry, обновлены пробеги и цены у 5 авто + добавлены 2 новых авто (Geely Monjaro, Hyundai Santa Fe). При синхронизации: 5 обновлено, 2 создано!
3. `data/1c_dump_partner.csv` — Партнерская CSV-выгрузка с разделителем `;` (5 автомобилей: BMW X5, Touareg, Forester, Changan CS55, Zeekr 001).
4. `data/1c_dump.json` — Расширенный набор из 16 автомобилей.

---

## 🧪 Запуск тестов

Для запуска набора автоматических тестов (unit + integration):

```bash
pytest tests/ -v
```

Тесты выполняются в изолированной `in-memory` базе SQLite и проверяют:
- Парсинг русских ключей 1С.
- Парсинг CSV с различными разделителями.
- Корректность вставки и обновления записей (upsert).
- Работу всех HTTP API эндпоинтов и возврат HTML-страницы.

---

## ⚙️ Переменные окружения

| Переменная | По умолчанию | Описание |
|---|---|---|
| `DATABASE_URL` | `sqlite+aiosqlite:///./db/autocheck.db` | URL подключения к БД (SQLite, PostgreSQL, MySQL) |
| `DUMP_FILE_PATH` | `./data/1c_dump.json` | Путь к основному файлу выгрузки 1С |
| `PARSE_INTERVAL_MINUTES` | `5` | Интервал автоматического парсинга (в минутах) |
| `DEBUG` | `False` | Режим отладки и подробного логирования SQL |
